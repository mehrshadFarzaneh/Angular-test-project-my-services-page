import { PriceTablePipe } from './price-table.pipe';

describe('PriceTablePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceTablePipe();
    expect(pipe).toBeTruthy();
  });
});
