import { DateJalaliPipe } from './date-jalali.pipe';

describe('DateJalaliPipe', () => {
  it('create an instance', () => {
    const pipe = new DateJalaliPipe();
    expect(pipe).toBeTruthy();
  });
});
